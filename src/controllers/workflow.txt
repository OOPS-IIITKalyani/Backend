new workflow ppl

- take the input 
- then make a unquie set os symotoms from it
-

- they have a function that goes through the user array and find out no of matches between the target diaeases and the array (easy)
-  that takes a String parameter newOption, which represents a new option to be added to a JSON file containing an array of options. 
- updates a JSON file with new disease information